# Core (cross-platform, required)
numpy>=1.24
pyyaml>=6.0

# On-device inference. onnxruntime-qnn ships the QNN (Hexagon NPU) Execution Provider
# for Snapdragon devices. On non-Snapdragon dev machines, plain onnxruntime works and
# FlowKeeper falls back to the CPU EP / heuristic classifier automatically.
onnxruntime>=1.17            ; platform_machine != "ARM64"
onnxruntime-qnn>=1.17        ; platform_machine == "ARM64"

# Live signal capture (Windows target for the challenge; optional elsewhere).
# FlowKeeper runs in --simulate mode without these installed.
pynput>=1.7                  ; sys_platform == "win32"
psutil>=5.9
mss>=9.0                     ; sys_platform == "win32"
pywin32>=306                 ; sys_platform == "win32"
winrt-Windows.UI.Notifications>=2.0 ; sys_platform == "win32"

# Model authoring / export (dev only)
scikit-learn>=1.3
skl2onnx>=1.16
